# Notebooks

Not yet built. This is Milestone 2 (BUILD_PLAN.md Section 12): a Colab-
ready `build-workbook.ipynb` porting the classification/gain-calculation/
export logic to plain Python functions, plus CSV/Excel/HTML parsing
(the three lightweight formats), tested against everything in
`../fixtures/`.

Do not start this before Milestone 1 (template + guided chat, all
profiles) is built and validated — the spreadsheet+chat path is the
reference implementation this notebook gets checked against.
