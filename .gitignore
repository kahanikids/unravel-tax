# Install & build (regenerated by npm ci / npm run build)
node_modules/
dist/
*.tsbuildinfo
coverage/

# Python
__pycache__/
*.py[cod]
notebooks/output/

# Logs & OS
*.log
.DS_Store
Thumbs.db

# Local IDE / AI tooling (not needed to run or for CI)
.cursor/
.claude/
.vscode/
*.code-workspace

# Secrets (none used today; belt for a public repo)
.env
.env.*
!.env.example

# Scratch / one-off scripts
*.tmp
*.tmp.*

# Internal dev docs (not needed by users, the app, or CI)
WORKING_PLAN.md
**/NOTES.md
**/DESIGN_NOTES.md

# Broken local git copies
.git-broken*/
